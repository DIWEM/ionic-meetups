import { EventEmitter, OnDestroy, OnChanges, SimpleChanges } from '@angular/core';
export declare class BarecodeScannerLivestreamComponent implements OnChanges, OnDestroy {
    type: string;
    valueChanges: EventEmitter<{}>;
    barecodeScanner: any;
    private started;
    private configQuagga;
    ngOnDestroy(): void;
    ngOnChanges(changes: SimpleChanges): void;
    private _init;
    start(): Promise<void>;
    stop(): void;
    retart(): void;
    isStarted(): boolean;
    onProcessed(result: any): any;
    onDetected(result: any): void;
}
